import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrapper: {
    flexDirection: 'row',
  },
  column: {
    flex: 1,
  },
  header: {
    paddingHorizontal: 10,
  },
});
